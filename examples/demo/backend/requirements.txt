# The demo uses the local guidebridge checkout:
#   pip install -e ../../../python[fastapi] -r requirements.txt
fastapi>=0.110
uvicorn>=0.29
# Optional, for POST /demo/chat (real LLM agent):
# langchain>=1.0
# langchain-anthropic>=0.3
